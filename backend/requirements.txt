# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Faz 0 (Local MVP)
#
# Faz 0'da OLMAYAN (kasıtlı):
#   - celery, redis      → Faz 1'de AbstractTaskRunner swap'ı (ADR-006)
#   - asyncpg            → Faz 1'de PostgreSQL geçişinde eklenir (ADR-005)
#   - alembic            → Faz 1'de migration yönetimi için eklenir
#   - PyJWT, bcrypt      → Faz 1'de auth aktif edilince eklenir (ADR-007)
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # Form ve dosya upload için

# Veritabanı (Faz 0: senkron SQLAlchemy + SQLite — kurulum gerektirmez)
sqlalchemy>=2.0.0

# Validation
pydantic>=2.7.0
pydantic-settings>=2.2.0

# PDF işleme
pdfplumber>=0.11.0
Pillow>=10.0.0               # pdfplumber bağımlılığı
psycopg2-binary>=2.9.9

openpyxl>=3.1.2
