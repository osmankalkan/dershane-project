# ─────────────────────────────────────────────────────────────────────────────
# requirements-dev.txt — Geliştirme araçları (tüm fazlar için ortak)
# ─────────────────────────────────────────────────────────────────────────────

-r requirements.txt          # Proje bağımlılıklarını içerir

# Test
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                # FastAPI test client için

# Kod kalitesi (mimari-sablon.md §1.3)
ruff>=0.4.0

# Git hook otomasyonu
pre-commit>=3.7.0
